version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused

  settings:
    govet:
      enable:
        - shadow
    errcheck:
      check-type-assertions: true
      check-blank: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
